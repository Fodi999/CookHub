<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Главная страница</title>
    
    <!-- Подключение общих стилей -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav/nav-header.css">
    <link rel="stylesheet" href="/css/nav/nav-bottom.css">
    <link rel="stylesheet" href="/css/nav/nav-responsive.css">

    <!-- Манифест для PWA -->
    <link rel="manifest" href="/manifest.json">
    
    <!-- Мета-теги для PWA -->
    <meta name="theme-color" content="#007bff">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">

    <!-- Подключение иконок для PWA -->
    <link rel="icon" sizes="192x192" href="/icons/icon-192x192.png">
    <link rel="icon" sizes="512x512" href="/icons/icon-512x512.png">
</head>
<body>
    <!-- Включаем навигацию -->
    <%- include('partials/nav'); %>

    <div class="container">
        <h1>Добро пожаловать на CookHub!</h1>
        <p>Это ваше сообщество для обмена сообщениями и управления контентом.</p>
        <div class="feed">

            <div class="chat-feed">
                <% messages.forEach(function(message) { %>
                    <div class="message">
                        <p><strong><%= message.username %>:</strong> <%= message.message %></p>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>

    <!-- Подключаем Service Worker -->
    <script>
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('/service-worker.js')
                    .then(registration => {
                        console.log('Service Worker зарегистрирован с областью:', registration.scope);
                    })
                    .catch(error => {
                        console.log('Регистрация Service Worker не удалась:', error);
                    });
            });
        }
    </script>
</body>
</html>






 